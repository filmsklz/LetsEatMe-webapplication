ngDescribe({
    name: 'Test all-merchant component',
    modules: 'app',
    element: '<all-merchant></all-merchant>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
