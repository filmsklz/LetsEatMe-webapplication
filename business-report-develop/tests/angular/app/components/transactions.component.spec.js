ngDescribe({
    name: 'Test transactions component',
    modules: 'app',
    element: '<transactions></transactions>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
