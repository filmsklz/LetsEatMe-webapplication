ngDescribe({
    name: 'Test dashboard-avg component',
    modules: 'app',
    element: '<dashboard-avg></dashboard-avg>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
