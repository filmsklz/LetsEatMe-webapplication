ngDescribe({
    name: 'Test nav-side component',
    modules: 'app',
    element: '<nav-side></nav-side>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
